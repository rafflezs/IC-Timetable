#include "Classes/input.h"
int main(){

    /* string fileName = "input.py";
    string command = "python3 ";
    command += fileName;
    system(command.c_str()); */

	srand(time(NULL));
    start();
    cout << "\033[0m";

    return 0;
}
